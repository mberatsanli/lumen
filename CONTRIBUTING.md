# Contributing

1. Create a focused branch.
2. Add tests for parser, cascade or layout changes.
3. Run `cargo fmt --all`.
4. Run `cargo clippy --workspace --all-targets`.
5. Run `cargo test --workspace`.

Keep pull requests small and explain any intentional deviation from browser standards.
